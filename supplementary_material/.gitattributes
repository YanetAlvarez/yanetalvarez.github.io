# Track mp4 with Git LFS (GIFs are small enough to commit directly)
*.mp4 filter=lfs diff=lfs merge=lfs -text
# GIFs are small enough to commit directly to GitHub
# *.gif filter=lfs diff=lfs merge=lfs -text
